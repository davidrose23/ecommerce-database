{
  "development": {
    "username": "${DB_USER}",
    "password": "${DB_PW}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  },
  "test": {
    "username": "${DB_USER}",
    "password": "${DB_PW}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  },
  "production": {
    "username": "${DB_USER}",
    "password": "${DB_PW}",
    "database": "${DB_NAME}",
    "host": "${DB_HOST}",
    "dialect": "mysql"
  }
}

  